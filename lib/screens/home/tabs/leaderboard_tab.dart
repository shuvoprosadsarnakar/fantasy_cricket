import 'package:flutter/material.dart';

class LeaderboardTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Icon(Icons.leaderboard))
      
    );
  }
}